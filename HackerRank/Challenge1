
(defn solveMeFirst [a b]
  (+ a b))

(defn -main []
  (let [a (Integer/parseInt (read-line))
        b (Integer/parseInt (read-line))]
    (println (solveMeFirst a b))))

(-main) ; Call the main function to start the program